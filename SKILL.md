---
name: scan-project-generate-prompts
description: >-
  扫描当前/现有项目工作区，输出 5 道 0-1 代码生成题、5 道 Feature 扩展题、
  1 道代码重构题、1 道代码测试题，共 12 道结构化提示词。
  每道题均锚定仓库内真实文件/模块/行为，强制达到「5 轮以上对话才能完成」的难度。
  触发词：扫描现有项目、扫描项目、根据仓库出题、扫描代码生成提示词、生成题目。
---

# 扫描项目生成提示词 Skill

## 核心原则：先扫描，后出题

**必须扫描用户当前指定的仓库/工作区中的真实代码与配置**，所有题目来源于扫描到的事实（文件、模块、函数、配置、依赖等）。  
若未指定路径，以当前工作区/仓库根为默认扫描范围。  
**禁止**在未扫描的情况下使用占位路径或凭空杜撰题目。

---

## 启用条件

当用户要求以下任一动作时启用：
- 「扫描现有项目」「扫描项目」
- 「根据代码/仓库出题」「生成一套提示词/题目」
- 「帮我出几道题」「基于这个仓库出题」

**固定输出配额**：  
5 道 0-1 代码生成题 + 5 道 Feature 扩展题 + 1 道代码重构题 + 1 道代码测试题 = **12 道**

---

## Repo ID 规范

**自选 Repo** 格式：`姓名拼音缩写 + Repo编号 + 题目编号`  
例：`wha-1-2`（武怀安自选/创建的第 1 个 Repo 第 2 题）  
注：一道题可对应多个 Session ID（即表格中的多条提交数据）

**题库 Repo** 格式：`题库 Repo ID + 题目编号`  
例：`A-1234-3`（领题表中 Repo ID 为 A-1234 的第 3 题）  
题库地址：[链接]

---

## 领题表使用规范

使用领题表中的 Repo 前：
1. 在「领题人」列填写姓名（人和 repo 一一对应）
2. 题目设计并提交后，在「提交表自动编号」列填入对应的自动编号（多个编号用中文逗号"，"隔开）

---

## 执行流程（必须按顺序）

### 1. 确认扫描对象
- 明确根目录（用户指定路径或当前仓库根）
- 确定 Repo ID（自选或题库）
- 输出「扫描摘要」（见产出结构）

### 2. 建立扫描范围
必须阅读的关键文件类型：
- `README`、`package.json`/`pyproject.toml`/`go.mod` 等包管理文件
- 主要源码目录（如 `src/`、`lib/`、`app/`）
- 入口文件、路由定义、数据模型、配置模块
- 测试目录（如 `tests/`、`__tests__/`）
- 若 monorepo：说明聚焦的子包及跨包关系

### 3. 抽取事实（仅来自当前仓库）
记录以下用于出题的素材：
- 模块边界、主要函数/类名（真实路径）
- 数据流、外部依赖、API 端点
- 错误处理缺口、事务边界问题
- 测试覆盖率薄弱处、并发/性能隐患
- 重复代码、过长函数、耦合过紧的模块

### 4. 按配额生成题目
- **0-1 生成（5 道）**：从零实现新功能/模块，必须与现有代码衔接
- **Feature 扩展（5 道）**：修改/扩展现有功能，需保持向后兼容
- **代码重构（1 道）**：行为不变，提升可维护性
- **代码测试（1 道）**：补充测试，覆盖真实缺口

### 5. 自检清单
- [ ] 总数 12 道
- [ ] 每道题至少对应一处已扫描的真实文件/行为（可溯源）
- [ ] 每道题满足 **5 轮以上对话才能完成** 的难度（见难度约束）
- [ ] 无通用面试题、无虚构路径
- [ ] 题目描述中**不出现具体文件路径、函数名、变量名**（改用业务语义或模块级描述）
- [ ] 每条提示词包含：具体目标、约束、可验收标准（或检查清单）
- [ ] 0-1 生成和 Feature 扩展的提示词中不包含背景描述

---

## 固定产出结构（必须严格按此顺序）

```markdown
## 扫描摘要
- 扫描对象：[仓库根路径]
- Repo ID：[自选 Repo ID 或题库 Repo ID]
- 主要技术栈：[如 TypeScript + React + Express + PostgreSQL]
- 本轮实际扫描过的路径与关键文件：[列表]
- 风险或复杂度要点：[一句话，如“多处缓存与数据库事务交错”]

---

## 0-1生成（5 道）

### 0-1生成 1
[提示词正文]

### 0-1生成 2
[提示词正文]

### 0-1生成 3
[提示词正文]

### 0-1生成 4
[提示词正文]

### 0-1生成 5
[提示词正文]

---

## Feature 扩展（5 道）

### Feature 1
[提示词正文]

### Feature 2
[提示词正文]

### Feature 3
[提示词正文]

### Feature 4
[提示词正文]

### Feature 5
[提示词正文]

---

## 代码重构（1 道）

### 重构题
[提示词正文]

---

## 代码测试（1 道）

### 测试题
[提示词正文]